export enum Schemas {
  User = 'user',
  Attachment = 'attachment',
  Creator = 'creator',
  Token = 'token',
  Project = 'project',
  Market = "market",
  Vault = "vault"
}

export enum Modules {
  Logger = 'winston',
  EthersProvider = 'EthersProvider',
}

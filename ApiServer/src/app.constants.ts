export enum Schemas {
  User = 'user',
  Attachment = 'attachment',
  Creator = 'creator',
  Token = 'token',
  Project = 'project'
}

export enum Modules {
  Logger = 'winston',
  EthersProvider = 'EthersProvider',
}

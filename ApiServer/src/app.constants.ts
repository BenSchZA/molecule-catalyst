export enum Schemas {
  User = 'User',
  Organization = 'Organization',
  Patent = 'Patent',
  Attachment = 'Attachment',
  Creator = 'Creator',
  Token = 'Token',
}

export enum Modules {
  Logger = 'winston',
  EthersProvider = 'EthersProvider',
}
